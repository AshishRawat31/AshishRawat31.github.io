contact:
  allowedFields: ["name", "email", "message"]
  branch: "master"
  commitMessage: "New Message"
  filename: "{@timestamp}"
  generatedFields:
    date:
      type: date
      options:
        format: "timestamp-seconds"
  moderation: false
  path: "_data/Messages"
