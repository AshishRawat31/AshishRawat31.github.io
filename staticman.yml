contact:
  allowedFields: ["name", "email", "message"]
  allowedOrigins: ["localhost", "ashishrawat31.github.io"]
  branch: "master"
  commitMessage: "New Message"
  filename: "{@id}"
  generatedFields:
    date:
      type: date
      options:
        format: "timestamp-seconds"
  moderation: false
  path: "_data/comments/{options.slug}"
