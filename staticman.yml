contact:
  allowedFields: ["name", "email", "message"]
  branch: "master"
  commitMessage: "New Message"
  filename: "{@timestamp}"
  format: "yaml"
  generatedFields:
    date:
      type: "date"
      options:
        format: "timestamp-seconds"
  moderation: false
  path: "_data/Messages"
